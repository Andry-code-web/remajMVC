<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
<link rel="stylesheet" href="/css/metodopago/metodopago.css">

<section class="banner" id="banner">
  <div id="carouselExampleInterval" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
      <div class="carousel-item active" data-bs-interval="2000">
        <img src="/img/Bannermejora.png" class="d-block w-100" alt="...">
      </div>
      <div class="carousel-item">
        <img src="/img/banner2.jpeg" class="d-block w-100" alt="...">
      </div>
      <div class="carousel-item">
        <img src="/img/banner3.jpeg" class="d-block w-100" alt="...">
      </div>
      <div class="carousel-item">
        <img src="/img/banner4.jpeg" class="d-block w-100" alt="...">
      </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleInterval" data-bs-slide="prev">
      <span class="carousel-icon" aria-hidden="true">
        <i class="bi bi-chevron-left"></i>
      </span>
      <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleInterval" data-bs-slide="next">
      <span class="carousel-next-icon" aria-hidden="true">
        <i class="bi bi-chevron-right"></i>
      </span>
      <span class="visually-hidden">Next</span>
    </button>
  </div>
</section>

<div class="slider_contenedor">
  <div class="slide-track">
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="terreno_rural">
        <img src="/img/Terreno rural.png" alt="">
        Terreno Rural
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="casa">
        <img src="/img/casa.png" alt="">
        Casa
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="cochera">
        <img src="/img/cochera.png" alt="">
        Cochera
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="departamento">
        <img src="/img/Departamentos.png" alt="">
        Departamento
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="terraza">
        <img src="/img/terraza.png" alt="">
        Terraza
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="terreno">
        <img src="/img/terreno.png" alt="">
        Terreno
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="local">
        <img src="/img/tienda.png" alt="">
        local
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="oficina">
        <img src="/img/oficina.png" alt="">
        Oficina
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="terreno_rural">
        <img src="/img/Terreno rural.png" alt="">
        Terreno Rural
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="casa">
        <img src="/img/casa.png" alt="">
        Casa
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="cochera">
        <img src="/img/cochera.png" alt="">
        Cochera
      </a>
    </div>
    <div class="slide_img">
      <a href="javascript:void(0)" data-categoria="departamento">
        <img src="/img/Departamentos.png" alt="">
        Departamento
      </a>
    </div>
  </div>
</div>

<div class="contenidoPrincipalPagina">
  <aside class="barraLateralFiltros">
    <h4>FILTROS</h4>
    <hr>
    <form action="/filtrar/" method="post" class="form_filtro">
      <div class="imputsContenedor">
        <div class="imputnumeroIdentificacionRemate">
          <label for="numeroIdentificacionRemate">Número de Remate</label>
          <input type="text" id="numeroIdentificacionRemate" name="id">
        </div>
        <div class="imputnumeroIdentificacionRemate">
          <label for="numeroExpedienteJudicial">Partida registral</label>
          <input type="text" id="numeroExpedienteJudicial" name="partida_registral">
        </div>
        <div class="imputnumeroIdentificacionRemate">
          <label for="valorBasePropiedad">Precio Base</label>
          <input type="text" id="valorBasePropiedad" name="precio">
        </div>
      </div>
      <div class="imputsContenedor">
        <div class="contenedor-campo-formulario-ubicacion">
          <label for="ubicacionGeograficaPropiedad">Ubicación</label>
          <select name="ubicacion" id="ubicacionGeograficaPropiedad" class="selector-desplegable-ubicacion">
            <option value="">Selecciona una ubicación</option>
            <option value="Amazonas">Amazonas</option>
            <option value="Ancash">Áncash</option>
            <option value="Apurimac">Apurímac</option>
            <option value="Arequipa">Arequipa</option>
            <option value="Ayacucho">Ayacucho</option>
            <option value="Cajamarca">Cajamarca</option>
            <option value="Callao">Callao</option>
            <option value="Cusco">Cusco</option>
            <option value="Huancavelica">Huancavelica</option>
            <option value="Huanuco">Huánuco</option>
            <option value="Ica">Ica</option>
            <option value="Junin">Junín</option>
            <option value="La Libertad">La Libertad</option>
            <option value="Lambayeque">Lambayeque</option>
            <option value="Lima">Lima</option>
            <option value="Loreto">Loreto</option>
            <option value="Madre de Dios">Madre de Dios</option>
            <option value="Moquegua">Moquegua</option>
            <option value="Pasco">Pasco</option>
            <option value="Piura">Piura</option>
            <option value="Puno">Puno</option>
            <option value="San Martin">San Martín</option>
            <option value="Tacna">Tacna</option>
            <option value="Tumbes">Tumbes</option>
            <option value="Ucayali">Ucayali</option>
          </select>
        </div>
        <div class="contenedor-campo-formulario-categoria">
          <label for="categoria">Categoría</label>
          <select name="categoria" id="categoria" class="selector-desplegable-categoria">
            <option value="">Selecciona una categoria</option>
            <option value="departamento">Departamento</option>
            <option value="cochera">Cochera</option>
            <option value="terreno">Terreno</option>
            <option value="residencial">Residencial</option>
            <option value="casa">Casa</option>
            <option value="local">Local</option>
            <option value="casa_playa">Casa de Playa</option>
            <option value="terreno_urbano">Terreno Urbano</option>
            <option value="terraza">Aires y Azoteas</option>
          </select>
        </div>
        <button type="submit" class="btn_filtrar">Buscar</button>
      </div>
    </form>
  </aside>
  <main class="listaSubastasDisponibles">
    <nav aria-label="Navegación de remates">
      <ul class="pagination justify-content-end">
        <li class="page-item <%= !pagination.hasPrevPage ? 'disabled' : '' %>">
          <a class="page-link" href="?page=<%= pagination.prevPage %>" <%= !pagination.hasPrevPage ? 'tabindex="-1" aria-disabled="true"' : '' %>>
            <span aria-hidden="true">&laquo;</span>
          </a>
        </li>
        <% for(let i = 1; i <= pagination.totalPages; i++) { %>
        <li class="page-item <%= pagination.currentPage === i ? 'active' : '' %>">
          <a class="page-link" href="?page=<%= i %>"><%= i %></a>
        </li>
        <% } %>
        <li class="page-item <%= !pagination.hasNextPage ? 'disabled' : '' %>">
          <a class="page-link" href="?page=<%= pagination.nextPage %>" <%= !pagination.hasNextPage ? 'tabindex="-1" aria-disabled="true"' : '' %>>
            <span aria-hidden="true">&raquo;</span>
          </a>
        </li>
      </ul>
    </nav>

    <% if (remates.length === 0) { %>
    <div class="alert alert-danger" role="alert">
      No hay remates disponibles en este momento. Por favor, intente más tarde o modifique sus criterios de búsqueda.
    </div>
    <% } else { %>
    <% remates.forEach(remate => { %>
    <div class="carta elementoSubastaIndividual">
      <div class="imagenPropiedadSubasta">
        <div class="contenedor_image">
          <% if (remate.imagenes.length > 0) { %>
            <div id="carouselRemate<%= remate.id %>" class="carousel slide" data-bs-ride="carousel">
              <div class="carousel-inner">
                <% remate.imagenes.forEach((imagen, index) => { %>
                  <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                    <a href="javascript:void(0);" style="text-decoration: none;" onclick="mostrarAlerta(<%= remate.id %>)">
                      <img src="data:image/jpeg;base64,<%= imagen %>" class="d-block w-100 imagen-propiedad" alt="Imagen del inmueble">
                    </a>
                  </div>
                <% }) %>
              </div>
              <!-- Controles de navegación -->
              <button class="carousel-control-prev" type="button" data-bs-target="#carouselRemate<%= remate.id %>" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Anterior</span>
              </button>
              <button class="carousel-control-next" type="button" data-bs-target="#carouselRemate<%= remate.id %>" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Siguiente</span>
              </button>
            </div>
          <% } else { %>
            <img src="/img/casa2.jfif" alt="Imagen no disponible" class="imagen-propiedad">
          <% } %>
        </div>
      </div>
      
      
      
      <div class="contenidoInformativoSubasta">
        <div class="detallesEspecificosSubasta">
          <a id="informacion_general_subasta" href="javascript:void(0);" class="informacionGeneralSubasta" onclick="mostrarAlerta(<%= remate.id %>)">

            <h2>Remate N° <%= String(remate.id).padStart(4, '0') %> <%= remate.convocatoria %> </h2>
            <p class="tipo_remate"><strong>TIPO DE REMATE:</strong> Extrajudicial</p>
            <p class="lugar"><strong>LUGAR:</strong>
              <%= remate.ubicacion %> Perú
            </p>
            <h3><strong>Presentación de Ofertas</strong></h3>
            <p class="hora_remate">
              <i class="ri-calendar-line"></i>
              <%= new Date(remate.fecha_remate).toLocaleDateString('es-PE', {
                    day: '2-digit',
                    month: '2-digit',
                    year: 'numeric'
                  }) %>
            </p>
            <p class="hora_remate">
              <i class="ri-time-line"></i>
              <% 
  let [hours, minutes, seconds] = remate.hora_remate.split(':'); 
  let date = new Date();
  date.setHours(hours, minutes, seconds);
%>
<%= date.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', hour12: true }) %>

            </p>
            <p class="descripcion_remate"><%= remate.descripcion %></p>
          </a>
          <%
                const estados = {
                  finalizado: {
                    clase: '',
                    texto: 'Remate Adjudicado'
                  },
                  en_curso: {
                    clase: 'bg-success',
                    texto: 'Publicacion e Inscripcion'
                  }
                }
                const estadoActual = estados[remate.estado];
              %>
          <% if (estadoActual) { %>
          <div class="estadoActualSubasta <%= estadoActual.clase %> text-light">
            <p class="text-uppercase fs-5"><strong><%= remate.estado %></strong></p>
            <p><%= estadoActual.texto %></p>
          </div>
          <% } %>
          <!-- boton me gusta -->
          <input type="checkbox" id="casilla-me-gusta-<%= remate.id %>" class="casilla-de-verificacion-me-gusta" data-remate-id="<%= remate.id %>">
          <label for="casilla-me-gusta-<%= remate.id %>" class="contenedor-boton-me-gusta">
            <span class="icono-corazon-me-gusta" style="height: 12px;" style="justify-content: center;">♥</span>
          </label>
          <!-- cierre de boton me gusta -->
        </div>
        <div class="contenedorBotonesAccion">
          <div class="precio_remate">

            <p>PRECIO BASE:<i class="fa-solid fa-gavel fa-beat icono-precio"></i>
            </p>
            <p>
              $ <%= remate.precios %>
            </p>
          </div>
          <div class="btn_acciones">
            <a id="informacion_general_subastaa" href="javascript:void(0);" class="botonAccionSubasta detalle-button" onclick="mostrarAlerta(<%= remate.id %>)"><i class="fa-solid fa-hammer fa-fade"> </i> Ingresar </a>
            <a href="#" id="botonAccionbasesDelRemate_<%= remate.id %>" class="botonAccionSubasta basesDelRemate-button"><i class="fa-solid fa-scale-balanced"></i> bases del remate</a>
            <a href="/en_vivo/detalles/<%= remate.id %>" class="botonAccionSubasta detalle-button" data-remate-id="<%= remate.id %>"> <i class="fas fa-info-circle"> </i> Detalles </a>
            <a href="#" class="botonAccionSubasta aviso-button" target="_blank" data-aviso-url="<%= remate.anexos && remate.anexos.length > 0 ? remate.anexos[0].papeles_inmuebles : '#' %>">
              <i class="fas fa-building"></i> Aviso </a>
          </div>
        </div>
      </div>
    </div>
    <% }) %>
    <% } %>

    <nav aria-label="Navegación de remates">
      <ul class="pagination justify-content-end">
        <li class="page-item <%= !pagination.hasPrevPage ? 'disabled' : '' %>">
          <a class="page-link" href="?page=<%= pagination.prevPage %>" <%= !pagination.hasPrevPage ? 'tabindex="-1" aria-disabled="true"' : '' %>>
            <span aria-hidden="true">&laquo;</span>
          </a>
        </li>
        <% for(let i = 1; i <= pagination.totalPages; i++) { %>
        <li class="page-item <%= pagination.currentPage === i ? 'active' : '' %>">
          <a class="page-link" href="?page=<%= i %>"><%= i %></a>
        </li>
        <% } %>
        <li class="page-item <%= !pagination.hasNextPage ? 'disabled' : '' %>">
          <a class="page-link" href="?page=<%= pagination.nextPage %>" <%= !pagination.hasNextPage ? 'tabindex="-1" aria-disabled="true"' : '' %>>
            <span aria-hidden="true">&raquo;</span>
          </a>
        </li>
      </ul>
    </nav>
  </main>
</div>

<!-- Contenedor para el modal de método de pago -->
<div class="payment-modal-overlay" id="metodo-pago-container" style="display: none;">
  <div class="payment-modal-content">
    <%- include('../layouts/Metodopago') %>
  </div>
</div>
<!-- icono nuevo -->
<div class="icon-container">
  <i class="fa-duotone fa-solid fa-hammer fa-beat-fade fa-2xl" style="--fa-primary-color: #581919; --fa-secondary-color: #936262;"></i>
</div>
<!-- fin del icono -->


<!-- Contenedor de bases del remate -->
<section id="tarjetaInformativaRemateContenedor" class="tarjeta-informativa-remate-contenedor">
  <%- include("../layouts/basesRemates") %>
</section>



<!-- script enlace de aviso -->
<script>
  const avisoButtons = document.querySelectorAll('.aviso-button');

  avisoButtons.forEach(button => {
    button.addEventListener('click', function(event) {
      event.preventDefault();
      const avisoUrl = this.getAttribute('data-aviso-url');
      if (avisoUrl && avisoUrl !== '#') {
        window.open(avisoUrl, '_blank');
      } else {
        Swal.fire({
          icon: 'error',
          title: 'Oops...',
          text: 'No hay enlace disponible para este aviso.',
        });
      }
    });
  });
</script>

<script>
  document.querySelectorAll('.slide_img a').forEach(item => {
    item.addEventListener('click', function(e) {
      e.preventDefault(); // Evita el comportamiento por defecto del enlace
      const categoria = item.getAttribute('data-categoria');
      window.location.href = `/filtrarG?categoria=${categoria}`; // Redirige a la ruta con el filtro
    });
  });
</script>

<!-- alerta al ingresar a la subastas  -->
<script>
  function mostrarAlerta(remateId) {
    Swal.fire({
      title: `Remate N° ${String(remateId).padStart(4, '0')}`,
      text: "Solo se puede acceder si realizaste el previo pago y estas correctamente validado",
      showCancelButton: true,
      showDenyButton: true,
      confirmButtonText: 'Siguiente',
      denyButtonText: 'Depositar',
      cancelButtonText: 'Cancelar'
    }).then((result) => {
      if (result.isConfirmed) {
        // Intentar acceder a la subasta
        fetch(`/auctions/${remateId}`)
          .then(response => {
            if (response.redirected) {
              // Si la respuesta es una redirección, significa que el usuario no está autenticado
              Swal.fire({
                title: 'INICIO DE SESIÓN OBLIGATORIA',
                text: 'Debes iniciar sesión para poder acceder a esta página',
                showCancelButton: true,
                confirmButtonText: 'Iniciar Sesión',
                cancelButtonText: 'Retroceder'
              }).then((result) => {
                if (result.isConfirmed) {
                  window.location.href = '/auth/login';
                } else if (result.dismiss === Swal.DismissReason.cancel) {
                  mostrarAlerta(remateId);
                }
              });
            } else {
              // Verificar oportunidades
              fetch('/check-opportunities', {
                  method: 'POST',
                  headers: {
                    'Content-Type': 'application/json'
                  },
                  body: JSON.stringify({
                    auctionId: remateId
                  })
                })
                .then(response => response.json())
                .then(data => {
                  if (data.success) {
                    // Si la validación es exitosa, redirigir al usuario a la subasta
                    window.location.href = `/auctions/${remateId}`;
                  } else {
                    // Si la validación falla, mostrar alerta
                    Swal.fire({
                      icon: "warning",
                      title: "Oops...",
                      text: `No estás validado para el Remate N° ${String(remateId).padStart(4, '0')}`,
                      showCancelButton: true,
                      confirmButtonText: 'Solicitar Acceso',
                      cancelButtonText: 'Retroceder',
                      footer: '<a target="_blank" href="https://wa.link/2tmvgy">Quiero ingresar al remate</a>'
                    }).then((result) => {
                      if (result.isConfirmed) {
                        document.getElementById('metodo-pago-container').style.display = 'block';
                        Swal.close(); // Cerrar la alerta
                      } else if (result.dismiss === Swal.DismissReason.cancel) {
                        mostrarAlerta(remateId);
                      }
                    });
                  }
                });
            }
          });
      } else if (result.isDenied) {
        document.getElementById('metodo-pago-container').style.display = 'block';
        Swal.close(); // Cerrar la alerta
      }
    });
  }
  document.addEventListener('DOMContentLoaded', () => {
    const btnRetroceder = document.getElementById('retroceder');
    btnRetroceder.addEventListener('click', () => {
      document.getElementById('metodo-pago-container').style.display = 'none';
      mostrarAlerta(remateId); // Ver la SweetAlert otra vez
    });
  });
</script>

<!-- verificacion de me gusta  -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const botonesMeGusta = document.querySelectorAll('.casilla-de-verificacion-me-gusta');
    botonesMeGusta.forEach((boton) => {
      const remateId = boton.getAttribute('data-remate-id');
      boton.id = `casilla-me-gusta-${remateId}`;
      const label = boton.nextElementSibling;
      label.setAttribute('for', `casilla-me-gusta-${remateId}`);

      // Verificar si el usuario ya ha dado like
      const userAuthenticated = <%= userAuthenticated ? 'true' : 'false' %>;
      if (userAuthenticated) {
        fetch(`/check-like?remateId=${remateId}`, {
            method: 'GET',
            headers: {
              'Content-Type': 'application/json'
            }
          })
          .then(response => response.json())
          .then(data => {
            if (data.liked) {
              boton.checked = true;
              label.classList.add('liked');
            }
          })
          .catch(error => {
            console.error('Error al verificar like:', error);
          });
      }

      // Añadir evento de clic para cada botón
      boton.addEventListener('change', (event) => {
        const isChecked = event.target.checked;

        if (!userAuthenticated) {
          event.target.checked = false; // Desmarcar el checkbox
          Swal.fire({
            title: 'Inicia sesión para dar Me gusta',
            text: `Debes iniciar sesión para darle me gusta a el Remate N° ${String(remateId).padStart(4, '0')}`,
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Iniciar sesión',
            cancelButtonText: 'Cancelar'
          }).then((result) => {
            if (result.isConfirmed) {
              window.location.href = '/auth/login';
            }
          });
        } else {
          fetch('/toggle-like', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json'
              },
              body: JSON.stringify({
                remateId
              })
            })
            .then(response => response.json())
            .then(data => {
              if (data.message === 'Like añadido') {
                label.classList.add('liked');
              } else {
                label.classList.remove('liked');
              }
            })
            .catch(error => {
              console.error('Error al manejar like:', error);
            });
        }
      });
    });
  });
</script>

<!-- script para activar la tarjeta de bases de remate -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const botones = document.querySelectorAll('.basesDelRemate-button');

    botones.forEach(boton => {
      boton.addEventListener('click', function(event) {
        event.preventDefault(); // Evita que el enlace navegue a otra página
        const contenedor = document.getElementById('tarjetaInformativaRemateContenedor');

        if (contenedor.style.display === 'none' || contenedor.style.display === '') {
          contenedor.style.display = 'block'; // Muestra el contenedor
        } else {
          contenedor.style.display = 'none'; // Oculta el contenedor
        }
      });
    });
  });
</script>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>