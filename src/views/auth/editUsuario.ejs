<!-- contenedorde editar usuario -->
<div class="contenedor-general">
  <div class="contenedor_formulario">
    <!-- Panel izquierdo con información del usuario -->
    <div class="left-panel">
      <div class="profile-section">
        <div class="header-content">
          <h2>Perfil del Usuario</h2>
          <p class="subtitle">INFORMACIÓN PERSONAL</p>
        </div>

        <div class="user-avatar">
          <div class="avatar-circle">
            <i class="bi bi-person-circle"></i>
          </div>
          <h3 class="username"><%= userData.usuario %></h3>
        </div>

        <div class="profile-info">
          <div class="info-item">
            <i class="bi bi-person-badge"></i>
            <p>Usuario desde: <span>2024</span></p>
          </div>
          <div class="info-item">
            <i class="bi bi-envelope-check"></i>
            <p>Email verificado</p>
          </div>
        </div>

        <div class="profile-actions">
          <a href="/" class="action-link">
            <i class="bi bi-house-door"></i>
            Ir al inicio
          </a>
          <a href="/auth/logout" class="action-link">
            <i class="bi bi-box-arrow-right"></i>
            Cerrar sesión
          </a>
        </div>
      </div>
    </div>

    <!-- Panel derecho con el formulario de edición -->
    <div class="right-panel">
      <div class="edit-form">
        <div class="form-header">
          <h1 class="main-title">
            <i class="bi bi-pencil-square"></i>
            Editar Perfil
          </h1>
          <p class="description">Actualiza tu información personal</p>
        </div>

        
        <form id="updateProfileForm" method="post" class="form-content">
          <div class="form-section">
            <div class="form-group">
              <label for="nombreUsuario">
                <i class="bi bi-person-fill"></i>
                Nombre de usuario
              </label>
              <input type="text" id="username" name="usuario" value="<%- userData.usuario%>" required>
            </div>
            <div class="form-group">
              <label for="correo">
                <i class="bi bi-envelope-fill"></i>
                Correo Electrónico
              </label>
              <input type="email" id="email"  name="correo" value="<%- userData.correo %>" required>
            </div>
            <div class="form-group">
              <label for="confirmar_correo">
                <i class="bi bi-envelope-check-fill"></i>
                Confirmar Correo
              </label>
              <input type="email" id="email" name="confirmar_correo" value="<%- userData.confirmar_correo %>"  required>
            </div>
            <div class="form-group">
              <label for="celular">
                <i class="bi bi-phone-fill"></i>
                Teléfono 
              </label>
              <input type="tel" id="celular"  name="celular" value="<%- userData.celular %>" required>
            </div>
          </div>
        
          <div class="button-group">
            <button type="submit" class="save-button">
              <i class="bi bi-check-circle"></i>
              Guardar Cambios
            </button>
            <a href="/" class="cancel-button">
              <i class="bi bi-x-circle"></i>
              Cancelar
            </a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>


<!-- COMPONENTE FOOTER -->
<%- include('../partials/footer') %>

<!-- ESTILOS EDITAR USUARIO -->
<link rel="stylesheet" href="/css/editUsuario/editUsuario.css">

<!-- Bootstrap Icons -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

<!-- SweetAlert2 -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>



<!-- script editar usuario -->
<script>
  document.getElementById('updateProfileForm').addEventListener('submit', async (e) => {
    e.preventDefault();
  
    const formData = new FormData(e.target);
    const data = {
      usuario: formData.get('usuario'),
      correo: formData.get('correo'),
      confirmar_correo: formData.get('confirmar_correo'),
      celular: formData.get('celular')
    };
  
    // Validaciones
    if (data.correo !== data.confirmar_correo) {
      Swal.fire({
        icon: 'error',
        title: 'Error',
        text: 'Los correos electrónicos no coinciden'
      });
      return;
    }
  
    const phoneRegex = /^9\d{8}$/;
    if (!phoneRegex.test(data.celular)) {
      Swal.fire({
        icon: 'error',
        title: 'Error',
        text: 'El número de teléfono debe comenzar con 9 y tener 9 dígitos'
      });
      return;
    }
  
    try {
      const response = await fetch('/auth/update-profile', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(data)
      });
  
      const result = await response.json();
  
      if (result.success) {
        Swal.fire({
          icon: 'success',
          title: '¡Éxito!',
          text: 'Perfil actualizado correctamente',
          showConfirmButton: false,
          timer: 1500
        }).then(() => {
          window.location.href = '/auth/login';
        });
      } else {
        Swal.fire({
          icon: 'error',
          title: 'Error',
          text: result.message || 'Error al actualizar el perfil'
        });
      }
    } catch (error) {
      console.error('Error:', error);
      Swal.fire({
        icon: 'error',
        title: 'Error',
        text: 'Hubo un error al actualizar el perfil'
      });
    }
  });
  </script>
